server:
  port: 8086

spring:
  application:
    name: social
  profiles:
    active: "prod"
  config:
    import: "optional:configserver:http://localhost:8071/"

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
  packages-to-scan:
    - com.oralie.social

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
    shutdown:
      enabled: true
  info:
    env:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}

endpoints:
  shutdown:
    enabled: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/

info:
  app:
    name: "social"
    description: "Social application"
    version: "1.0.0"

logging:
  level:
    com:
      oralie:
        social: DEBUG

aws:
  region:
    static: ap-southeast-1
#    static: ${AWS_REGION}
  bucket:
#    name: ${AWS_BUCKET_NAME}
    name: oralie-bucket
  credentials:
    accessKey: AKIAZKDIDJ2SBJPDTMQX
    secretKey: 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
#    accessKey: ${AWS_ACCESS_KEY}
#    secretKey: ${AWS_SECRET_KEY}

